---
export interface Props {
  href: string;
}

const { href } = Astro.props;

const isActive = href === Astro.url.pathname;
---

<a href={href} class:list={['block p-4', isActive && 'underline']}>
  <slot />
</a>
